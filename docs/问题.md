### bochs启动空硬盘不会出现报错
在 `bochsrc`配置文件中的`romimage`项,不要使用latest,改用legacy.

例如:
```c
romimage: file="/usr/share/bochs/BIOS-bochs-legacy" address=0x00000000, options=none
```

### 结构体内存结构
1. 每个成员偏移地址是自身类型大小的整数倍
2. 在内存中成员按顺序排列,低位在前